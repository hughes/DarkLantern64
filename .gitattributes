# Keep text assets and source hashes stable across Windows and Unix checkouts.
* text=auto eol=lf
*.blend binary
*.png binary
*.jpg binary
# Unified patches use a leading context marker on otherwise empty lines.
tools/tiny3d/patches/*.patch whitespace=-blank-at-eol
